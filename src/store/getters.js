const getters = {
  defaultAccout: state => state.app.defaultAccout,
  tokenData: state => state.app.tokenData,
  pairData:state => state.app.pairData,
  tolerance:state => state.app.tolerance,
  language:state => state.app.language
}
export default getters
